# ABNORMAL_RULES

본 문서는 FOM File Set을 구성하는 6개 파일 중 **`manual abnormal.csv` 단일 파일**의 명세서이다. FOM File Set 전체 구성과 나머지 파일(생산·비가동·불량·기준값·비용)의 역할은 `FOM_RULES.md` 1절을 참조한다.

**`manual abnormal.csv`** 는 Manual QPR의 `부적합` 컬럼에 입력되는 부적합 요인명을 표준화하기 위한 **부적합 유형 정의 Master 파일**이다. 본 문서는 이 파일의 컬럼 구조, QPR과의 연결 규칙, 데이터 품질 규칙을 정의한다.

> **문서 범위**: `ABNORMAL_RULES.md`는 `manual abnormal.csv` 파일 하나의 구조와 QPR 연결 규칙만 다룬다.
> FOM File Set 전체 맥락은 `FOM_RULES.md`, QPR 구조는 `QPR_RULES.md`를 참조한다.

> **구조 참고**: 본 파일은 `manual reject.csv`(`REJECT_RULES.md` 참조)와 동일하게 **요인명 1개 컬럼으로 구성된 Master 파일**이다. 대상 요인이 불량이 아닌 부적합이라는 점만 다르며, 나머지 구조·연결·검증 규칙은 동일한 패턴을 따른다.

> **용어 참고 (부적합 영문 표기)**: 설명·서술 목적으로는 "Nonconformity"와 "Abnormal"을 혼용해도 무방하다. 단, FOM File Set 구성 요소(파일명·식별자)는 항상 `abnormal`을 사용한다(`FOM_RULES.md` 1.1절 참조).

> **용어 참고 (예시값)**: 본 문서의 예시 값(부적합 유형명 등)은 설명을 위한 가상의 값이다. 실제 프로젝트 데이터에 등장하는 고유명사·제품코드·거래처명을 그대로 사용하지 않는다.

---

## 0. 파일 형식

Abnormal 파일은 CSV 형식으로 제공된다.

- 파일 확장자: .csv
- 인코딩: UTF-8 또는 EUC-KR (프로젝트별 상이할 수 있음)
- 구분자: 콤마(,) 기본 (프로젝트에 따라 탭 또는 세미콜론 가능)

### 처리 규칙

- 모든 컬럼 값은 문자열로 읽는다.
- 빈 값("")은 무시한다.
- 값 앞뒤 공백은 trim 후 비교한다.

## 1. Abnormal 파일 기본 구조

Manual Abnormal 파일은 현재 다음 1개 컬럼으로 구성된다.

| No | 컬럼명 | 설명 |
| -- | --- | --- |
| 1 | 요인명 | 부적합 요인(유형)의 표준 명칭 |

```text
요인명
치수불량
표면이상
포장불량
자재부적합
```

- 헤더 행은 `요인명` 한 컬럼만 유효 컬럼으로 처리한다.
- 파일에 헤더만 존재하고 데이터 행이 없는 경우(요인명 목록이 아직 정의되지 않은 상태)는 구조 오류가 아니라 **미등록 상태**로 처리한다. 이 경우 QPR에 `부적합` 값이 하나라도 존재하면 전량 `Unknown Abnormal` 경고 대상이 된다(4절 참조).
- Excel 저장 과정에서 함께 남는 후행 빈 컬럼(콤마만 있는 컬럼)은 로드 시 제거 대상으로 처리하며, 데이터로 해석하지 않는다.
- 행 하나는 하나의 부적합 요인만 정의한다. 하나의 행에 복수 요인명을 콤마·슬래시 등으로 나열해서는 안 된다.

## 2. Key 정의 및 QPR 연결 규칙

Abnormal 파일과 QPR 파일은 다음 단일 값으로 연결한다.

> **요인명 = QPR.부적합**

QPR의 `부적합` 컬럼에 입력된 값은 반드시 Abnormal 파일의 `요인명` 목록에 존재해야 한다.

- QPR.부적합 = Abnormal.요인명

**Abnormal 파일은 `부적합` 요인만을 대상으로 한다.** 비가동 요인명은 `manual downtime.csv`, 불량 요인명은 `manual reject.csv`에서 별도로 관리하며, 세 파일을 서로 혼용해서는 안 된다.

QPR에 `부적합` 값이 존재하는데 Abnormal 목록에 없는 경우, 해당 값은 미등록 요인으로 처리하며 `Unknown Abnormal` 경고 대상으로 분류한다(4절 참조).

## 3. 컬럼별 규칙

### 3.1 요인명 Rule

- 하나의 부적합 유형은 하나의 명칭만 사용한다. 동일 의미를 여러 표기로 나누어 등록하지 않는다.

  허용: `치수불량`
  금지: `치수불량`, `치수 불량`, `치수부적합`, `치수이상(부적합)` 를 동시에 등록

- 생산현장에서 실제 사용하는 공식 용어를 사용한다. 한글/영문 중 하나를 프로젝트 기준으로 통일한다.
- 앞뒤 공백을 포함한 공백 중복 표기를 허용하지 않는다(`포장불량`과 ` 포장불량`을 별도 항목으로 등록하지 않는다).
- 대소문자·특수문자 표기를 표준화한다(`NG`와 `ng`를 동시에 사용하지 않는다).
- **불량(Reject)과의 구분을 명확히 한다.** 부적합은 규격·절차 위반(예: 검사 누락, 자재 부적합, 공정 조건 이탈)을 의미하며, 완제품의 품질 결함을 의미하는 불량(Reject)과 개념이 다르다. 동일 현상을 프로젝트 내에서 `불량`과 `부적합` 양쪽 목록에 중복 등록하지 않는다. 구분 기준은 프로젝트별로 사전에 명시적으로 합의해야 한다.

## 4. 데이터 검증 규칙

| Rule | 내용 |
| --- | --- |
| R001 | 요인명은 파일 내에서 유일해야 한다(중복 등록 금지). |
| R002 | 빈 요인명은 허용하지 않는다. |
| R003 | 공백만 있는 값은 허용하지 않는다. |
| R004 | QPR의 `부적합` 값은 반드시 Abnormal 파일의 `요인명`에 존재해야 하며, 없으면 `Unknown Abnormal`로 경고 처리한다. |
| R005 | 요인명의 대소문자·표기법은 프로젝트 내에서 표준화한다(3.1절). |

## 5. 삭제 금지 및 사용여부 Rule

현재 파일 구조에는 사용여부를 나타내는 별도 컬럼이 없다. 따라서 더 이상 사용하지 않는 부적합 유형이라도 파일에서 행을 삭제하지 않는 것을 원칙으로 한다.

- 과거 QPR 데이터에서 이미 사용된 요인명을 Abnormal 파일에서 삭제하면, 과거 데이터의 `부적합` 값이 미등록 요인(R004 위반)으로 전환되어 이력 데이터의 정합성이 깨진다.
- 사용 중단된 요인을 구분해야 하는 경우, 파일에서 삭제하는 대신 별도 컬럼(예: `사용여부`)을 통해 관리하는 것을 권장한다(8절 참조).

## 6. 손실비용 연계 Rule

Abnormal 파일은 부적합 유형(요인명)만 정의하며, **금액 정보를 저장하지 않는다.**

부적합 영향금액은 다음과 같이 QPR과 Cost 파일을 연결하여 산출한다.

```
부적합 영향금액 = 부적합수량(QPR) × 단가(manual cost.csv)
```

단가 연결 규칙은 `COST_RULES.md`를 따른다. Abnormal 파일에 단가·금액 컬럼을 추가해서는 안 된다.

## 7. FOM 분석 연계

Abnormal 파일의 요인명은 FOM Code `#4000`(부적합) 분석의 기준 코드로 사용된다.

| FOM Code | 분석 |
| --- | --- |
| #4100 | 부적합 종합 |
| #4200 | 제품별 부적합 |
| #4300 | 설비별 부적합 |
| #4400 | 작업자별 부적합 |
| #4500 | 부적합요인별 분석 |

`#4500` 분석은 Abnormal 파일에 등록된 요인명 단위로 QPR의 `부적합수량`을 집계하는 것을 기준으로 한다. FOM Code 전체 체계는 `FOM_RULES.md` 10절을 참조한다.

## 8. 확장 검토 항목 (현재 필수 구조 아님)

다음 항목은 현재 FOM 파일 형식의 필수 Rule에 포함하지 않는다. 향후 확장 검토 대상으로만 남겨둔다.

| 컬럼 | 설명 |
| --- | --- |
| AbnormalCode | 요인명을 대체/보완하는 코드 값 |
| AbnormalGroup | 검사·자재·공정조건·문서 등 상위 분류 |
| 4M Category | 요인을 Man/Machine/Material/Method 관점으로 분류 |
| Severity | 부적합 심각도(예: Low/Medium/High/Critical) |
| 사용여부(UseYN) | 요인 사용 중단 여부를 Y/N으로 관리 (5절의 삭제 대체 수단) |

이러한 확장 컬럼이 도입되면 4M 분석, Pareto 분석, 손실비용 분석과의 연계가 더 정교해질 수 있으나, 도입 시에도 다음 원칙은 유지한다.

- 요인명(AbnormalName)은 여전히 QPR의 `부적합` 컬럼과 연결되는 유일한 식별 값이어야 한다.
- 금액 정보는 여전히 Abnormal 파일이 아닌 Cost 파일에서만 관리한다(6절).
- 확장 컬럼 도입 시 기존 요인명 값과 QPR 이력 데이터의 연결이 끊어지지 않도록 마이그레이션 규칙을 별도로 정의해야 한다.

## 9. 요약

> **Manual Abnormal 파일은 요인명을 유일 식별자로 하여 부적합 유형을 정의하는 Master 파일이며, Manual QPR의 `부적합` 컬럼 값은 반드시 이 파일에 등록된 요인명과 일치해야 한다. 금액은 저장하지 않으며, 손실금액은 Manual Cost 파일과 연결하여 별도로 산출한다.**
