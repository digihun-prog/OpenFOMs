# COST_RULES

본 문서는 FOM File Set을 구성하는 6개 파일 중 **`manual cost.csv` 단일 파일**의 명세서이다. FOM File Set 전체 구성과 나머지 파일(생산·비가동·불량·부적합·기준값)의 역할은 `FOM_RULES.md` 1절을 참조한다.

**`manual cost.csv`** 는 Manual QPR의 생산실적·비가동·불량·부적합 데이터를 경제적 가치로 환산하기 위한 **제품별 단가 기준정보 파일**이다. 본 문서는 이 파일의 컬럼 구조, Key 정의, QPR과의 연결 규칙, 데이터 품질 규칙을 정의한다.

> **문서 범위**: `COST_RULES.md`는 `manual cost.csv` 파일 하나의 구조와 QPR 연결 규칙만 다룬다.
> FOM File Set 전체 맥락은 `FOM_RULES.md`, QPR 구조는 `QPR_RULES.md`를 참조한다.

> **용어 참고**: 본 문서의 예시 값(설비명, 제품명, 공정명 등)은 설명을 위한 가상의 값이다. 실제 프로젝트 데이터에 등장하는 고유명사·제품코드·거래처명을 그대로 사용하지 않는다.

---

## 0. 파일 형식

Cost 파일은 CSV 형식으로 제공된다.

- 파일 확장자: .csv
- 인코딩: UTF-8 또는 EUC-KR (프로젝트별 상이할 수 있음)
- 구분자: 콤마(,) 기본 (프로젝트에 따라 탭 또는 세미콜론 가능)

### 처리 규칙

- 모든 컬럼 값은 문자열로 읽은 후, 단가 컬럼만 후처리에서 숫자형으로 변환한다.
- 빈 값("")은 NULL(미확인)로 해석하며 0으로 강제 변환하지 않는다.
- 정의되지 않은 빈 컬럼(예: 저장 과정에서 생성되는 `Unnamed` 계열 컬럼)은 로드 시 제거 대상으로 처리한다.

## 1. Cost 파일 기본 구조

Manual Cost 파일은 다음 5개 컬럼으로 구성한다. 컬럼 순서는 아래를 원칙으로 한다.

| No | 컬럼명 | 설명 |
| -- | --- | --- |
| 1 | 대분류 | 공정 계층 Level1 (QPR의 대분류와 동일 개념, 프로젝트별 의미 상이) |
| 2 | 중분류 | 공정 계층 Level2 (QPR의 중분류와 동일 개념, 프로젝트별 의미 상이) |
| 3 | 소분류 | 공정 계층 Level3 (QPR의 소분류와 동일 개념, 프로젝트별 의미 상이) |
| 4 | 제품명 | 단가를 적용할 제품 |
| 5 | 단가 | 제품 1단위의 기준가격 |

Cost 파일의 대분류·중분류·소분류는 QPR의 공정 계층(Process Hierarchy)과 동일한 개념이다. Level별 실제 의미(설비, 라인, 공정, 제품군 등)는 프로젝트마다 다르게 정의될 수 있으며, 이는 `QPR_RULES.md` 2절의 공정 계층 규칙을 그대로 따른다.

## 2. Key 정의 및 QPR 연결 규칙

Cost 파일과 QPR 파일은 다음 복합키로 연결한다.

> **대분류 + 중분류 + 소분류 + 제품명**

QPR의 `제품` 컬럼은 Cost 파일의 `제품명` 컬럼과 대응한다.

다음 네 개 값이 모두 일치할 때 단가가 정상적으로 연결된 것으로 판단한다.

- QPR.대분류 = Cost.대분류
- QPR.중분류 = Cost.중분류
- QPR.소분류 = Cost.소분류
- QPR.제품 = Cost.제품명

**제품명만을 기준으로 단가를 연결해서는 안 된다.** 동일한 제품이라도 생산 설비, 공정 또는 제품군(즉 공정 계층 Level이 다른 경우)에 따라 단가가 다르게 설정될 수 있기 때문이다.

예시 (가상 데이터):

| 대분류 | 중분류 | 소분류 | 제품명 | 단가 |
| --- | --- | --- | --- | ---: |
| 설비A | 공정1 | 유형1 | 제품X | 1,835 |
| 설비B | 공정1 | 유형2 | 제품X | 1,063 |
| 설비C | 공정1 | 유형1 | 제품X | 1,835 |
| 설비D | 공정1 | 유형2 | 제품X | 1,063 |

동일한 제품명(`제품X`)이 서로 다른 공정 계층 조합에서 여러 번 등장하며 단가도 다르다. `제품명=제품X`만으로 단가를 조회하면 어떤 값을 적용해야 하는지 결정할 수 없으므로, 반드시 복합키 전체를 기준으로 조회해야 한다.

## 3. 컬럼별 규칙

### 3.1 대분류 Rule

대분류는 QPR에서 사용하는 공정 계층 Level1과 동일한 명칭을 사용한다.

- QPR의 대분류 명칭과 완전히 동일하게 입력한다.
- 앞뒤 공백을 허용하지 않는다.
- 동일 대상에 복수의 명칭을 사용하지 않는다(약칭과 정식명칭 혼용 금지).
- 번호·기호 표기 방식을 통일한다.

예: `설비1호기`와 `설비 1호기`는 서로 다른 값으로 인식될 수 있으므로 혼용하지 않는다.

`외주`와 같이 내부 생산이 아닌 외부 조달 구간을 구분하는 값을 대분류로 사용할 수 있다.

### 3.2 중분류 Rule

중분류는 QPR의 공정 계층 Level2와 동일한 값을 사용한다. 중분류 값은 QPR과 Cost 파일에서 철자, 공백 및 특수문자까지 동일해야 한다.

### 3.3 소분류 Rule

소분류는 QPR의 공정 계층 Level3과 동일한 값을 사용한다. 제품군, 세부 공정 또는 생산 형태를 구분하는 값이다.

동일한 제품명이라도 소분류가 다르면 서로 다른 가격정보로 관리할 수 있다. 따라서 소분류를 생략하거나 제품명만으로 단가를 조회해서는 안 된다.

### 3.4 제품명 Rule

Cost 파일의 제품명은 QPR의 `제품` 컬럼과 동일한 명칭을 사용한다.

- 앞뒤 공백을 제거한다.
- 연속 공백을 사용하지 않는다.
- 하이픈, 괄호, 쉼표 및 방향 표시(좌/우 구분 등)를 QPR과 동일하게 사용한다.
- 제품 속성 표기(사양·규격 코드 등)를 임의로 생략하지 않는다.
- 여러 제품을 하나의 제품명으로 묶어 입력하지 않는다.

동일 계열이지만 표기가 다른 값은 서로 다른 제품명으로 처리될 수 있다. 예를 들어 세부 사양 표기가 하나라도 다르거나 공백 개수가 다르면 별도 제품명으로 간주된다.

QPR에 `제품A외 N종`과 같이 여러 제품을 하나의 실적행으로 합쳐 기록한 값(복합 제품명)은 Cost 파일의 개별 제품명과 직접 연결되지 않는다. 이러한 데이터는 대표제품 단가 적용 여부 또는 제품별 분리 기준을 별도로 정의해야 한다(5절 참조).

### 3.5 단가 Rule

단가는 해당 복합키에 대응하는 제품 1단위의 가격을 입력한다.

정상 입력:
- 숫자만 입력한다 (예: `1573`, `1063`, `2000`).

비정상 입력(Rule 위반으로 처리):
- 통화기호·쉼표·단위 문자를 포함한 값 (예: `1,573원`, `₩1063`)
- 확정되지 않은 값을 문자로 표기한 값 (예: `미정`, `약 2000`)

단가는 0보다 큰 값을 원칙으로 하며, 단가를 확인할 수 없는 경우 빈값으로 관리하고 미확인 대상으로 분류한다(6절 참조).

## 4. 중복 Rule

동일한 복합키(`대분류 + 중분류 + 소분류 + 제품명`)에는 하나의 단가만 존재해야 한다.

네 개 항목이 모두 같은 행이 두 개 이상 존재해서는 안 된다. 제품명이 같더라도 대분류, 중분류 또는 소분류가 다르면 별도의 가격정보로 등록할 수 있다.

## 5. 복수 제품 실적 처리 Rule

QPR에 하나의 실적행이 여러 제품을 대표하는 복합 제품명(예: `제품A외 5종`)으로 기록된 경우, 이는 Cost 파일의 단가와 1:1로 직접 연결되지 않는다.

> 복수 제품 생산실적은 제품별 실적 행으로 분리하여 기록하는 것을 원칙으로 한다.

분리가 불가능한 경우에는 대표 제품 단가를 적용할지, 별도의 대표단가 산정 근거를 마련할지를 프로젝트 단위로 명시적으로 정의해야 하며, 자동으로 임의 단가를 적용해서는 안 된다.

## 6. 빈값 Rule

대분류, 중분류, 소분류 및 제품명은 필수항목으로 관리한다.

단가가 없는 항목도 Cost 파일에 등록할 수 있으나, 손실비용 산출에서는 미확인 단가로 처리한다. 단가가 빈값인 제품에 대해서는 다음 중 하나의 상태로 별도 관리하는 것을 권장한다.

- 단가 확인 필요
- 외주 단가 미확정
- 신규제품
- 가격정보 미수집
- 분석 제외

**단가가 없는 값을 0원으로 자동 변환해서는 안 된다.** 0원은 실제 가격이 0원이라는 의미와 미확인 상태를 구분할 수 없게 하기 때문이다.

## 7. 불필요 컬럼 Rule

Manual Cost 파일에는 정의되지 않은 빈 컬럼을 포함하지 않는 것을 원칙으로 한다. 최종 헤더는 다음과 같이 구성한다.

```text
대분류,중분류,소분류,제품명,단가
```

## 8. 단가의 의미 Rule

Cost 파일에는 단가 종류를 나타내는 별도 필드가 없다. 따라서 파일만으로는 이 단가가 판매단가, 생산단가, 제조원가, 납품단가, 표준단가 중 무엇인지 판단할 수 없다.

> 본 Manual Cost 파일의 단가는 제품 1개당 판매단가를 기준으로 하는 것을 기본 원칙으로 한다.

실제 값이 판매단가가 아닌 다른 기준(예: 표준 제조원가)이라면, 해당 프로젝트의 문서 또는 `config.json` 주석 등에 명시적으로 기록해야 한다. 단가 기준을 구분하는 필드가 필요한 경우는 향후 확장 사항으로 별도 정의한다(9절 참조).

## 9. 손실비용 적용

Manual Cost 파일의 단가는 Manual QPR의 생산실적 및 생산성 저해요인을 금액으로 환산할 때 사용한다. 기본 적용식은 다음과 같다.

- 생산금액 = 실적수량 × 단가
- 불량 손실금액 = 불량수량 × 단가
- 부적합 영향금액 = 부적합수량 × 단가
- 비가동 생산기회손실금액 = 비가동 예상손실수량 × 단가

Manual Cost 파일 자체에는 비가동 예상손실수량, 재작업비 또는 공헌이익 정보가 포함되어 있지 않다. 따라서 이 파일만으로 실제 회계상 손실비용을 모두 산출할 수 있다고 해석해서는 안 되며, Cost 파일이 제공하는 값은 FOM 분석을 위한 **제품별 기준단가**로만 사용한다.

## 10. 확장 예정 항목 (현재 필수 구조 아님)

다음 항목은 현재 FOM 파일 형식의 필수 Rule에 포함하지 않는다. 향후 확장 검토 대상으로만 남겨둔다.

- 제품명 단독 매칭 방식
- 단위(Unit) 컬럼
- 단가 종류 구분(CostType) 컬럼
- 적용 시작일(StartDate) 등 이력 관리 컬럼

현재 파일세트 기준 Manual Cost 파일의 필수 구조는 1절에 정의한 5개 컬럼이다.

## 11. 요약

> **Manual Cost 파일은 대분류·중분류·소분류·제품명을 복합키로 하여 해당 생산조건의 제품 단가를 관리하며, Manual QPR의 동일 복합키와 정확히 일치할 때만 단가를 적용한다.**
