# DOWNTIME_RULES

본 문서는 FOM File Set을 구성하는 6개 파일 중 **`manual downtime.csv` 단일 파일**의 명세서이다. FOM File Set 전체 구성과 나머지 파일(생산·불량·부적합·기준값·비용)의 역할은 `FOM_RULES.md` 1절을 참조한다.

**`manual downtime.csv`** 는 Manual QPR의 `비가동` 컬럼에 입력되는 비가동 요인명을 표준화하기 위한 **비가동 유형 정의 Master 파일**이다. 본 문서는 이 파일의 컬럼 구조, QPR과의 연결 규칙, 데이터 품질 규칙을 정의한다.

> **문서 범위**: `DOWNTIME_RULES.md`는 `manual downtime.csv` 파일 하나의 구조와 QPR 연결 규칙만 다룬다.
> FOM File Set 전체 맥락은 `FOM_RULES.md`, QPR 구조는 `QPR_RULES.md`를 참조한다.

> **구조 참고**: 본 파일은 `manual reject.csv`(`REJECT_RULES.md` 참조)와 동일하게 **요인명 1개 컬럼으로 구성된 Master 파일**이다. 대상 요인이 불량이 아닌 비가동이라는 점만 다르며, 나머지 구조·연결·검증 규칙은 동일한 패턴을 따른다.

> **용어 참고**: 본 문서의 예시 값(비가동 유형명 등)은 설명을 위한 가상의 값이다. 실제 프로젝트 데이터에 등장하는 고유명사·제품코드·거래처명을 그대로 사용하지 않는다.

---

## 0. 파일 형식

Downtime 파일은 CSV 형식으로 제공된다.

- 파일 확장자: .csv
- 인코딩: UTF-8 또는 EUC-KR (프로젝트별 상이할 수 있음)
- 구분자: 콤마(,) 기본 (프로젝트에 따라 탭 또는 세미콜론 가능)

### 처리 규칙

- 모든 컬럼 값은 문자열로 읽는다.
- 빈 값("")은 무시한다.
- 값 앞뒤 공백은 trim 후 비교한다.

## 1. Downtime 파일 기본 구조

Manual Downtime 파일은 현재 다음 1개 컬럼으로 구성된다.

| No | 컬럼명 | 설명 |
| -- | --- | --- |
| 1 | 요인명 | 비가동 요인(유형)의 표준 명칭 |

```text
요인명
설비고장
금형교환
로봇티칭
계획정지
결원
청소
```

- 헤더 행은 `요인명` 한 컬럼만 유효 컬럼으로 처리한다.
- **헤더 명칭 변형 허용**: 현장 원천 파일에서는 헤더가 `요인명` 대신 `비가동`처럼 요인 유형명 자체로 기록되는 경우가 있다. 파일 내 컬럼이 1개뿐이면 헤더 문자열과 무관하게 해당 컬럼을 `요인명` 컬럼으로 처리한다. 단, 저장 시에는 `요인명`으로 표준화하는 것을 권장한다.
- Excel 저장 과정에서 함께 남는 후행 빈 컬럼(콤마만 있는 컬럼)은 로드 시 제거 대상으로 처리하며, 데이터로 해석하지 않는다.
- 행 하나는 하나의 비가동 요인만 정의한다. 하나의 행에 복수 요인명을 콤마·슬래시 등으로 나열해서는 안 된다.

## 2. Key 정의 및 QPR 연결 규칙

Downtime 파일과 QPR 파일은 다음 단일 값으로 연결한다.

> **요인명 = QPR.비가동**

QPR의 `비가동` 컬럼에 입력된 값은 반드시 Downtime 파일의 `요인명` 목록에 존재해야 한다.

- QPR.비가동 = Downtime.요인명

**Downtime 파일은 `비가동` 요인만을 대상으로 한다.** 불량 요인명은 `manual reject.csv`, 부적합 요인명은 `manual abnormal.csv`에서 별도로 관리하며, 세 파일을 서로 혼용해서는 안 된다.

QPR에 `비가동` 값이 존재하는데 Downtime 목록에 없는 경우, 해당 값은 미등록 요인으로 처리하며 `Unknown Downtime` 경고 대상으로 분류한다(4절 참조).

## 3. 컬럼별 규칙

### 3.1 요인명 Rule

- 하나의 비가동 유형은 하나의 명칭만 사용한다. 동일 의미를 여러 표기로 나누어 등록하지 않는다.

  허용: `설비고장`
  금지: `설비고장`, `설비 고장`, `고장(설비)`, `설비트러블` 를 동시에 등록

- 생산현장에서 실제 사용하는 공식 용어를 사용한다. 한글/영문 중 하나를 프로젝트 기준으로 통일한다.
- 앞뒤 공백을 포함한 공백 중복 표기를 허용하지 않는다(`계획정지`와 ` 계획정지`를 별도 항목으로 등록하지 않는다).
- 대소문자·특수문자 표기를 표준화한다.
- **원인계 요인과 관리계 요인을 구분하지 않고 동일 목록에 등록한다.** 설비 고장과 같은 이상 정지(비계획)와 `계획정지`·`결원`과 같은 관리적 정지(계획/준계획)가 하나의 `요인명` 목록에 함께 존재할 수 있다. 유형 구분이 필요한 경우 8절의 확장 컬럼(DowntimeType)을 통해 별도 관리하는 것을 권장하며, 현재 필수 구조에는 포함하지 않는다.

## 4. 데이터 검증 규칙

| Rule | 내용 |
| --- | --- |
| R001 | 요인명은 파일 내에서 유일해야 한다(중복 등록 금지). |
| R002 | 빈 요인명은 허용하지 않는다. |
| R003 | 공백만 있는 값은 허용하지 않는다. |
| R004 | QPR의 `비가동` 값은 반드시 Downtime 파일의 `요인명`에 존재해야 하며, 없으면 `Unknown Downtime`으로 경고 처리한다. |
| R005 | 요인명의 대소문자·표기법은 프로젝트 내에서 표준화한다(3.1절). |

## 5. 삭제 금지 및 사용여부 Rule

현재 파일 구조에는 사용여부를 나타내는 별도 컬럼이 없다. 따라서 더 이상 사용하지 않는 비가동 유형이라도 파일에서 행을 삭제하지 않는 것을 원칙으로 한다.

- 과거 QPR 데이터에서 이미 사용된 요인명을 Downtime 파일에서 삭제하면, 과거 데이터의 `비가동` 값이 미등록 요인(R004 위반)으로 전환되어 이력 데이터의 정합성이 깨진다.
- 사용 중단된 요인을 구분해야 하는 경우, 파일에서 삭제하는 대신 별도 컬럼(예: `사용여부`)을 통해 관리하는 것을 권장한다(8절 참조).

## 6. 손실비용 연계 Rule

Downtime 파일은 비가동 유형(요인명)만 정의하며, **금액 정보와 예상손실수량을 저장하지 않는다.**

비가동 생산기회손실금액은 다음과 같이 QPR·Cost 파일과 연결하여 산출한다.

```
비가동 생산기회손실금액 = 비가동 예상손실수량 × 단가(manual cost.csv)
```

- `비가동 예상손실수량`은 QPR의 `비가동시간`과 해당 설비·제품의 표준 생산속도(Cycle Time 등)로부터 별도 산출되는 값이며, Downtime 파일이나 Cost 파일 자체에는 포함되어 있지 않다(`COST_RULES.md` 9절 참조).
- 단가 연결 규칙은 `COST_RULES.md`를 따른다. Downtime 파일에 단가·예상손실수량 컬럼을 추가해서는 안 된다.

## 7. FOM 분석 연계

Downtime 파일의 요인명은 FOM Code `#2000`(비가동) 분석의 기준 코드로 사용된다.

| FOM Code | 분석 |
| --- | --- |
| #2100 | 비가동 종합 |
| #2200 | 제품별 비가동 |
| #2300 | 설비별 비가동 |
| #2400 | 작업자별 비가동 |
| #2500 | 비가동요인별 분석 |

`#2500` 분석은 Downtime 파일에 등록된 요인명 단위로 QPR의 `비가동시간`을 집계하는 것을 기준으로 한다. FOM Code 전체 체계는 `FOM_RULES.md` 10절을 참조한다.

## 8. 확장 검토 항목 (현재 필수 구조 아님)

다음 항목은 현재 FOM 파일 형식의 필수 Rule에 포함하지 않는다. 향후 확장 검토 대상으로만 남겨둔다.

| 컬럼 | 설명 |
| --- | --- |
| DowntimeCode | 요인명을 대체/보완하는 코드 값 |
| DowntimeType | 계획정지 / 비계획정지(고장) / 준계획정지 등 정지 성격 구분 |
| DowntimeGroup | 설비계·품질계·관리계 등 상위 분류 |
| 4M Category | 요인을 Man/Machine/Material/Method 관점으로 분류 |
| 사용여부(UseYN) | 요인 사용 중단 여부를 Y/N으로 관리 (5절의 삭제 대체 수단) |

이러한 확장 컬럼이 도입되면 정지유형별 분석, Pareto 분석, 가동률 손실 구조 분석과의 연계가 더 정교해질 수 있으나, 도입 시에도 다음 원칙은 유지한다.

- 요인명(DowntimeName)은 여전히 QPR의 `비가동` 컬럼과 연결되는 유일한 식별 값이어야 한다.
- 금액·예상손실수량 정보는 여전히 Downtime 파일이 아닌 QPR·Cost 파일 조합에서만 산출한다(6절).
- 확장 컬럼 도입 시 기존 요인명 값과 QPR 이력 데이터의 연결이 끊어지지 않도록 마이그레이션 규칙을 별도로 정의해야 한다.

## 9. 요약

> **Manual Downtime 파일은 요인명을 유일 식별자로 하여 비가동 유형을 정의하는 Master 파일이며, Manual QPR의 `비가동` 컬럼 값은 반드시 이 파일에 등록된 요인명과 일치해야 한다. 금액은 저장하지 않으며, 비가동 손실금액은 QPR의 비가동시간과 Manual Cost 파일을 연결하여 별도로 산출한다.**
