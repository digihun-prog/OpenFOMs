# LIMIT_RULES

본 문서는 FOM File Set을 구성하는 6개 파일 중 **`manual limit.csv` 단일 파일**의 명세서이다. FOM File Set 전체 구성과 나머지 파일(생산·비가동·불량·부적합·비용)의 역할은 `FOM_RULES.md` 1절을 참조한다.

**`manual limit.csv`** 는 QPR 실적 데이터가 "정상 범위 안에 있는가"를 판정하기 위한 **관리 기준값(OPI, Operational Performance Indicator) 정의 파일**이다. 다른 File Set 구성원(QPR·Downtime·Reject·Abnormal·Cost)이 현장에서 "무엇이 발생했는가"를 기록하는 반면, Limit 파일은 데이터를 새로 생성하지 않고 **"어느 수준부터 관리가 필요한가"의 기준선만 제공**한다. 본 문서는 이 파일의 컬럼 구조, Key 정의, QPR과의 연결 규칙, 데이터 품질 규칙을 정의한다.

> **문서 범위**: `LIMIT_RULES.md`는 `manual limit.csv` 파일 하나의 구조와 QPR 연결 규칙만 다룬다.
> FOM File Set 전체 맥락은 `FOM_RULES.md`, QPR 구조는 `QPR_RULES.md`, 금액 기준은 `COST_RULES.md`를 참조한다.

> **용어 참고**: 본 문서의 예시 값(설비명, 제품명, 공정명 등)은 설명을 위한 가상의 값이다. 실제 프로젝트 데이터에 등장하는 고유명사·제품코드·거래처명을 그대로 사용하지 않는다.

---

## 0. 파일 형식

Limit 파일은 CSV 형식으로 제공된다.

- 파일 확장자: .csv
- 인코딩: UTF-8 또는 EUC-KR (프로젝트별 상이할 수 있음)
- 구분자: 콤마(,) 기본 (프로젝트에 따라 탭 또는 세미콜론 가능)

### 처리 규칙

- Key 컬럼(Shift~제품)은 문자열로 읽는다.
- 기준값 컬럼(달성률(최소)/달성률(최대)/비가동율/불량율/부적합율)은 후처리에서 숫자형(%)으로 변환한다.
- 빈 값("")은 NULL(기준 미설정)로 해석하며 0으로 강제 변환하지 않는다. 0은 "허용 한계가 0%"라는 실제 값이고, 빈 값은 "기준이 아직 정의되지 않음"이라는 의미이므로 서로 구분해야 한다.
- Excel 저장 과정에서 함께 남는 후행 빈 컬럼(콤마만 있는 컬럼)은 로드 시 제거 대상으로 처리하며, 데이터로 해석하지 않는다.

## 1. Limit 파일 기본 구조

Manual Limit 파일은 다음 11개 컬럼으로 구성한다. 컬럼 순서는 아래를 원칙으로 한다.

| No | 컬럼명 | 설명 |
| -- | --- | --- |
| 1 | Shift | 기준값을 적용할 Shift (QPR의 Shift와 동일 개념) |
| 2 | 대분류 | 공정 계층 Level1 (QPR의 대분류와 동일 개념, 프로젝트별 의미 상이) |
| 3 | 중분류 | 공정 계층 Level2 (QPR의 중분류와 동일 개념, 프로젝트별 의미 상이) |
| 4 | 소분류 | 공정 계층 Level3 (QPR의 소분류와 동일 개념, 프로젝트별 의미 상이) |
| 5 | 설비 | 기준값을 적용할 설비 (QPR의 설비와 동일 개념) |
| 6 | 제품 | 기준값을 적용할 제품 (QPR의 제품과 동일 개념) |
| 7 | 달성률(최소) | 생산 달성률 하한 기준값(%). 미달 시 관리 대상 |
| 8 | 달성률(최대) | 생산 달성률 상한 참고값(%). 초과 시 이상치(계획수량 오류 등) 검토 대상 |
| 9 | 비가동율 | 비가동시간 비율 상한 기준값(%). 초과 시 관리 대상 |
| 10 | 불량율 | 불량수량 비율 상한 기준값(%). 초과 시 관리 대상 |
| 11 | 부적합율 | 부적합수량 비율 상한 기준값(%). 초과 시 관리 대상 |

```text
Shift,대분류,중분류,소분류,설비,제품,달성률(최소),달성률(최대),비가동율,불량율,부적합율
주간,,,,,,95,105,3,0.5,0.2
야간,,,,설비A,,90,110,5,1.0,0.5
```

- 헤더 표기 표준화: 원천 파일에는 `달성률(최소)`와 `달성율(최대)`처럼 "률/율" 표기가 혼용되어 나타나는 경우가 있다. 저장 시에는 `달성률(최소)` / `달성률(최대)`로 표기를 통일한다.
- Excel 저장 과정에서 함께 남는 후행 빈 컬럼(콤마만 있는 컬럼)은 로드 시 제거 대상으로 처리하며, 데이터로 해석하지 않는다.
- 행 하나는 하나의 Key 조합(2절)에 대한 기준값 세트를 정의한다.
- 기준값이 없는 항목(예: 특정 라인에 부적합 기준이 없음)은 해당 컬럼을 빈 값으로 둔다. 0을 대신 입력하지 않는다(0절 참조).

## 2. Key 정의 및 QPR 연결 규칙

Limit 파일과 QPR 파일은 다음 복합키로 연결한다.

> **Shift + 대분류 + 중분류 + 소분류 + 설비 + 제품**

QPR의 `Key 정의`(`QPR_RULES.md` 2절)는 8개 항목(일자·Shift·공정 계층·설비·제품·작업자)이지만, Limit 파일의 Key는 그중 **일자와 작업자를 제외한 6개 항목**만 사용한다.

- 관리 기준값은 특정 날짜 하루에 국한되지 않고 지속적으로 적용되는 기준선이므로 `일자`를 Key에 포함하지 않는다.
- 관리 기준값은 개인이 아닌 라인·설비·제품 단위로 설정하는 것이 원칙이므로 `작업자`를 Key에 포함하지 않는다.

### 2.1 와일드카드(공통 적용) 규칙

Limit 파일의 Key 컬럼(Shift~제품)은 QPR과 달리 **빈 값을 허용**하며, 빈 값은 "해당 레벨 전체에 공통 적용"을 의미하는 와일드카드로 처리한다.

- 6개 Key 컬럼이 모두 빈 값인 행 → 프로젝트 전체 공통 기준
- `대분류`만 채워지고 나머지가 빈 값인 행 → 해당 대분류 전체에 공통 적용
- 6개 Key 컬럼이 모두 채워진 행 → 해당 조합에만 적용되는 개별 기준

### 2.2 매칭 우선순위

QPR의 한 작업 단위(Key)에 대해 Limit 파일에서 기준값을 조회할 때는 **더 구체적으로 일치하는 행을 우선 적용**한다.

1. Shift+대분류+중분류+소분류+설비+제품 6개 항목이 모두 일치하는 행
2. 일부 항목만 지정되고 나머지가 와일드카드(빈 값)인 행 중 가장 많은 항목이 일치하는 행
3. 6개 항목이 모두 빈 값인 전사 공통 기준 행
4. 일치하는 행이 없는 경우 → 기준 미설정으로 처리하며, 해당 QPR 실적은 판정 대상에서 제외한다(오류로 중단하지 않는다)

동일한 구체성 수준에서 두 개 이상의 행이 동시에 일치해서는 안 된다(4절 R003 참조).

## 3. 컬럼별 규칙

### 3.1 Key 컬럼 Rule

- Shift, 대분류, 중분류, 소분류, 설비, 제품 값은 QPR에서 사용하는 명칭과 완전히 동일하게 입력한다(철자·공백·특수문자 포함).
- 앞뒤 공백을 허용하지 않는다.
- 약칭과 정식명칭을 혼용하지 않는다.

### 3.2 달성률(최소) / 달성률(최대) Rule

- 백분율 숫자만 입력한다(예: `95`, `90.5`). `%` 기호나 문자열을 포함하지 않는다.
- 달성률(최소) ≤ 달성률(최대) 관계를 만족해야 한다. 위반 시 데이터 품질 경고로 처리한다(4절 R004).
- 달성률(최대)는 목표 초과 생산이나 계획수량 입력 오류를 탐지하기 위한 참고 상한이며, 반드시 100을 의미하지 않는다.

### 3.3 비가동율 / 불량율 / 부적합율 Rule

- 백분율 숫자만 입력하며, `%` 기호나 문자열을 포함하지 않는다.
- 0 이상의 값을 원칙으로 한다.
- 각 값은 QPR에서 계산되는 다음 실적 비율과 비교되는 **상한 기준값**이다.

```
비가동율(실적) = 비가동시간 합계 ÷ 작업시간 합계 × 100
불량율(실적)   = 불량수량 합계   ÷ 실적수량 합계 × 100
부적합율(실적) = 부적합수량 합계 ÷ 실적수량 합계 × 100
```

- 세 항목은 QPR·Downtime·Reject·Abnormal 파일에서 이미 산출된 실적 비율과 대응하며, Limit 파일 자체에는 실적 수치를 저장하지 않는다.

## 4. 데이터 검증 규칙

| Rule | 내용 |
| --- | --- |
| R001 | 동일한 Key 조합(Shift+대분류+중분류+소분류+설비+제품, 와일드카드 포함)은 파일 내에서 유일해야 한다(중복 등록 금지). |
| R002 | 기준값 컬럼(달성률(최소)/달성률(최대)/비가동율/불량율/부적합율)은 숫자 또는 빈 값만 허용한다. |
| R003 | 동일한 구체성 수준에서 두 개 이상의 행이 동일 QPR Key에 동시에 일치해서는 안 된다(2.2절). |
| R004 | 달성률(최소) > 달성률(최대)인 행은 데이터 품질 경고 대상으로 분류한다. |
| R005 | 기준값이 빈 값인 경우 0으로 자동 변환하지 않는다(0절). |

## 5. Cost 파일과의 차이

Limit 파일과 Cost 파일(`COST_RULES.md`)은 모두 QPR 실적과 결합되는 기준정보 파일이지만 역할이 다르다.

| 구분 | Cost 파일 | Limit 파일 |
| --- | --- | --- |
| 질문 | 불량 1건이 발생하면 얼마의 손실인가? | 불량률 1%가 허용 가능한 수준인가? |
| 산출물 | 금액(손실비용, 생산금액) | 판정(정상 / 관리 대상) |
| Key | 대분류+중분류+소분류+제품명 (4개, 와일드카드 없음) | Shift+대분류+중분류+소분류+설비+제품 (6개, 와일드카드 허용) |

두 파일을 혼용하여 Limit 파일에 단가를, Cost 파일에 기준값을 입력해서는 안 된다.

## 6. FOM 분석 연계

Limit 파일의 기준값은 QPR 기반 실적 집계 결과와 비교되어 `TB_WARNING`에 경고를 적재하는 데 사용된다.

```
QPR 실적 집계 (달성률 / 비가동율 / 불량율 / 부적합율)
        │
        ▼
Limit 파일에서 해당 Key의 기준값 조회 (2.2절 매칭 우선순위)
        │
        ▼
비교 판정
  - 달성률 < 달성률(최소)         → 관리 대상 (미달)
  - 달성률 > 달성률(최대)         → 이상치 검토 대상 (초과)
  - 비가동율/불량율/부적합율 > 기준값 → 관리 대상 (초과)
        │
        ▼
TB_WARNING 적재
```

현재 파일 구조는 지표별로 **단일 상한/하한 기준값**만 가지므로 판정은 "기준 이내 / 기준 초과(미달)"의 이분법으로 처리한다. 목표·경고·위험의 3단계 판정은 현재 필수 구조가 아니며 7절의 확장 검토 대상이다.

## 7. 확장 검토 항목 (현재 필수 구조 아님)

다음 항목은 현재 FOM 파일 형식의 필수 Rule에 포함하지 않는다. 향후 확장 검토 대상으로만 남겨둔다.

| 항목 | 설명 |
| --- | --- |
| 3단계 기준값 (목표/경고/위험) | 지표별로 단일 상한값 대신 목표·경고·위험 3단계 임계값을 관리하는 구조. 예: 불량율 목표 0.5% / 경고 1.0% / 위험 2.0% |
| KPI 확장 | 설비가동률(OEE), 납기준수율 등 현재 4개 지표(달성률·비가동율·불량율·부적합율) 외의 KPI에 대한 기준값 관리 |
| Objective Data 연계 KPI | 에너지 사용량, 단위생산당 전력(kWh/EA), CO₂ 배출량 등 Objective Data 축과 연계된 기준값 관리(`FOM_RULES.md` 6절의 Objective Data 원칙 참조) |
| 적용 시작일(StartDate) | 기준값의 이력 관리(기간별로 다른 기준값 적용) |

이러한 확장 컬럼이 도입되더라도 다음 원칙은 유지한다.

- Key는 여전히 QPR·Downtime·Reject·Abnormal의 대응 컬럼과 동일한 명칭을 사용해야 한다.
- 금액 정보는 Limit 파일이 아닌 Cost 파일에서만 관리한다(5절).
- 확장 시에도 Limit 파일은 데이터를 생성하지 않고 기준값만 제공하는 참조 파일이라는 성격을 유지한다(본 문서 서두 참조).

## 8. 요약

> **Manual Limit 파일은 Shift+대분류+중분류+소분류+설비+제품을 복합키(와일드카드 허용)로 하여 달성률·비가동율·불량율·부적합율의 관리 기준값을 정의하는 참조 파일이며, QPR 실적 집계 결과와 비교되어 정상/관리 대상 여부를 판정하는 데 사용된다. 금액은 저장하지 않으며, 손실비용 산출은 Manual Cost 파일이 담당한다.**
