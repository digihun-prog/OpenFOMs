# REJECT_RULES

본 문서는 FOM File Set을 구성하는 6개 파일 중 **`manual reject.csv` 단일 파일**의 명세서이다. FOM File Set 전체 구성과 나머지 파일(생산·비가동·부적합·기준값·비용)의 역할은 `FOM_RULES.md` 1절을 참조한다.

**`manual reject.csv`** 는 Manual QPR의 `불량` 컬럼에 입력되는 불량 요인명을 표준화하기 위한 **불량 유형 정의 Master 파일**이다. 본 문서는 이 파일의 컬럼 구조, QPR과의 연결 규칙, 데이터 품질 규칙을 정의한다.

> **문서 범위**: `REJECT_RULES.md`는 `manual reject.csv` 파일 하나의 구조와 QPR 연결 규칙만 다룬다.
> FOM File Set 전체 맥락은 `FOM_RULES.md`, QPR 구조는 `QPR_RULES.md`를 참조한다.

> **용어 참고**: 본 문서의 예시 값(불량 유형명 등)은 설명을 위한 가상의 값이다. 실제 프로젝트 데이터에 등장하는 고유명사·제품코드·거래처명을 그대로 사용하지 않는다.

---

## 0. 파일 형식

Reject 파일은 CSV 형식으로 제공된다.

- 파일 확장자: .csv
- 인코딩: UTF-8 또는 EUC-KR (프로젝트별 상이할 수 있음)
- 구분자: 콤마(,) 기본 (프로젝트에 따라 탭 또는 세미콜론 가능)

### 처리 규칙

- 모든 컬럼 값은 문자열로 읽는다.
- 빈 값("")은 무시한다.
- 값 앞뒤 공백은 trim 후 비교한다.

## 1. Reject 파일 기본 구조

Manual Reject 파일은 현재 다음 1개 컬럼으로 구성된다.

| No | 컬럼명 | 설명 |
| -- | --- | --- |
| 1 | 요인명 | 불량 요인(유형)의 표준 명칭 |

```text
요인명
예타
찍힘
결육
...
```

- 헤더 행은 `요인명` 한 컬럼만 유효 컬럼으로 처리한다.
- Excel 저장 과정에서 함께 남는 후행 빈 컬럼(콤마만 있는 컬럼)은 로드 시 제거 대상으로 처리하며, 데이터로 해석하지 않는다.
- 행 하나는 하나의 불량 요인만 정의한다. 하나의 행에 복수 요인명을 콤마·슬래시 등으로 나열해서는 안 된다.

## 2. Key 정의 및 QPR 연결 규칙

Reject 파일과 QPR 파일은 다음 단일 값으로 연결한다.

> **요인명 = QPR.불량**

QPR의 `불량` 컬럼에 입력된 값은 반드시 Reject 파일의 `요인명` 목록에 존재해야 한다.

- QPR.불량 = Reject.요인명

**Reject 파일은 `불량` 요인만을 대상으로 한다.** 비가동 요인명은 `manual downtime.csv`, 부적합 요인명은 `manual abnormal.csv`에서 별도로 관리하며, 세 파일을 서로 혼용해서는 안 된다.

QPR에 `불량` 값이 존재하는데 Reject 목록에 없는 경우, 해당 값은 미등록 요인으로 처리하며 `Unknown Reject` 경고 대상으로 분류한다(4절 참조).

## 3. 컬럼별 규칙

### 3.1 요인명 Rule

- 하나의 불량 유형은 하나의 명칭만 사용한다. 동일 의미를 여러 표기로 나누어 등록하지 않는다.

  허용: `찍힘`
  금지: `찍힘`, `찍힘불량`, `찍힘 발생`, `찍힘(외관)` 를 동시에 등록

- 생산현장에서 실제 사용하는 공식 용어를 사용한다. 한글/영문 중 하나를 프로젝트 기준으로 통일한다(예: `크랙` 또는 `Crack` 중 하나만 사용).
- 앞뒤 공백을 포함한 공백 중복 표기를 허용하지 않는다(`찍힘`과 ` 찍힘`을 별도 항목으로 등록하지 않는다).
- 대소문자·특수문자 표기를 표준화한다(`NG`와 `ng`를 동시에 사용하지 않는다).

## 4. 데이터 검증 규칙

| Rule | 내용 |
| --- | --- |
| R001 | 요인명은 파일 내에서 유일해야 한다(중복 등록 금지). |
| R002 | 빈 요인명은 허용하지 않는다. |
| R003 | 공백만 있는 값은 허용하지 않는다. |
| R004 | QPR의 `불량` 값은 반드시 Reject 파일의 `요인명`에 존재해야 하며, 없으면 `Unknown Reject`로 경고 처리한다. |
| R005 | 요인명의 대소문자·표기법은 프로젝트 내에서 표준화한다(3.1절). |

## 5. 삭제 금지 및 사용여부 Rule

현재 파일 구조에는 사용여부를 나타내는 별도 컬럼이 없다. 따라서 더 이상 사용하지 않는 불량 유형이라도 파일에서 행을 삭제하지 않는 것을 원칙으로 한다.

- 과거 QPR 데이터에서 이미 사용된 요인명을 Reject 파일에서 삭제하면, 과거 데이터의 `불량` 값이 미등록 요인(R004 위반)으로 전환되어 이력 데이터의 정합성이 깨진다.
- 사용 중단된 요인을 구분해야 하는 경우, 파일에서 삭제하는 대신 별도 컬럼(예: `사용여부`)을 통해 관리하는 것을 권장한다(6절 참조).

## 6. 손실비용 연계 Rule

Reject 파일은 불량 유형(요인명)만 정의하며, **금액 정보를 저장하지 않는다.**

불량 손실금액은 다음과 같이 QPR과 Cost 파일을 연결하여 산출한다.

```
불량 손실금액 = 불량수량(QPR) × 단가(manual cost.csv)
```

단가 연결 규칙은 `COST_RULES.md`를 따른다. Reject 파일에 단가·금액 컬럼을 추가해서는 안 된다.

## 7. FOM 분석 연계

Reject 파일의 요인명은 FOM Code `#3000`(불량) 분석의 기준 코드로 사용된다.

| FOM Code | 분석 |
| --- | --- |
| #3100 | 불량 종합 |
| #3200 | 제품별 불량 |
| #3300 | 설비별 불량 |
| #3400 | 작업자별 불량 |
| #3500 | 불량요인별 분석 |

`#3500` 분석은 Reject 파일에 등록된 요인명 단위로 QPR의 불량수량을 집계하는 것을 기준으로 한다. FOM Code 전체 체계는 `FOM_RULES.md`를 참조한다.

## 8. 확장 검토 항목 (현재 필수 구조 아님)

다음 항목은 현재 FOM 파일 형식의 필수 Rule에 포함하지 않는다. 향후 확장 검토 대상으로만 남겨둔다.

| 컬럼 | 설명 |
| --- | --- |
| RejectCode | 요인명을 대체/보완하는 코드 값 |
| RejectGroup / DefectGroup | 외관·치수·조립·가공 등 상위 분류 |
| 4M Category | 요인을 Man/Machine/Material/Method 관점으로 분류 |
| Severity | 불량 심각도(예: Low/Medium/High/Critical) |
| 사용여부(UseYN) | 요인 사용 중단 여부를 Y/N으로 관리 (5절의 삭제 대체 수단) |

이러한 확장 컬럼이 도입되면 4M 분석, Pareto 분석, 손실비용 분석과의 연계가 더 정교해질 수 있으나, 도입 시에도 다음 원칙은 유지한다.

- 요인명(RejectName)은 여전히 QPR의 `불량` 컬럼과 연결되는 유일한 식별 값이어야 한다.
- 금액 정보는 여전히 Reject 파일이 아닌 Cost 파일에서만 관리한다(6절).
- 확장 컬럼 도입 시 기존 요인명 값과 QPR 이력 데이터의 연결이 끊어지지 않도록 마이그레이션 규칙을 별도로 정의해야 한다.

## 9. 요약

> **Manual Reject 파일은 요인명을 유일 식별자로 하여 불량 유형을 정의하는 Master 파일이며, Manual QPR의 `불량` 컬럼 값은 반드시 이 파일에 등록된 요인명과 일치해야 한다. 금액은 저장하지 않으며, 손실비용은 Manual Cost 파일과 연결하여 별도로 산출한다.**
